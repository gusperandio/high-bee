enum Variant { def, black, info, success, warning, danger }

enum Size { small, medium, large, full }

enum RadiusBtn { none, small, medium, large }

enum TypeWidget { low, high }

enum BadgeAlignment { top, bottom }

enum BadgeVariant { def, alert }

enum AlignTitle { top, bottom }

enum RadiusImage { any, md, lg }

enum LogoColor { white, blue }
