import 'package:flutter/material.dart';

class InfoRulesPage extends StatelessWidget {
  const InfoRulesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}